# 复制为 infra/hermes/.env，切勿提交真实密钥。
# 两个密钥都建议使用：openssl rand -hex 32
HERMES_DASHBOARD_SESSION_TOKEN=请替换为随机会话令牌
READYWORK_BRIDGE_SECRET=请替换为随机桥接密钥

# 容器访问宿主机 Readywork API 的地址。
READYWORK_API_URL=http://host.docker.internal:4173

# 可选：使容器写入的文件归当前宿主机用户所有。
HERMES_UID=10000
HERMES_GID=10000
